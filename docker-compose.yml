version: "3.8"

services:
  AI-Sales-Assistants:
    build: .
    container_name: AI-Sales-Assistants
    env_file:
      - .env
    ports:
      - "8081:8081"
    volumes:
      - .:/app

  # ==========================================
  # OPTIONAL: Local Database Development Fallbacks
  # (Uncomment below if you want to run databases locally instead of Cloud)
  # ==========================================
  # db:
  #   image: postgres:15-alpine
  #   container_name: sales-db
  #   restart: always
  #   ports:
  #     - "5432:5432"
  #   environment:
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: password
  #     POSTGRES_DB: sales_copilot
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #
  # neo4j:
  #   image: neo4j:5.20.0-community
  #   container_name: neo4j
  #   ports:
  #     - "7687:7687"
  #     - "7474:7474"
  #   environment:
  #     - NEO4J_AUTH=neo4j/password
  #   volumes:
  #     - neo4j_data:/data
  #
  # qdrant:
  #   image: qdrant/qdrant:latest
  #   container_name: qdrant
  #   ports:
  #     - "6333:6333"
  #     - "6334:6334"
  #   volumes:
  #     - qdrant_storage:/qdrant/storage

# volumes:
#   qdrant_storage:
#   neo4j_data:
#   postgres_data:


